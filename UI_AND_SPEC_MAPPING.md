# คู่มืออธิบายหน้าเว็บ: ชี้จุดบนเว็บและจับคู่ตรงตามแผนโครงงาน ขั้นที่ 1
## โครงงาน: ระบบจัดการห้องสมุด (Library Management System)

> **ตำแหน่งโปรเจกต์เว็บ**: `D:\library-management-system`  
> **ไฟล์ต้นทางที่ใช้อ้างอิง**: `D:\แผนโครงงาน_ขั้นที่1.md`  
> **URL หน้าเว็บขณะรัน**: `http://localhost:3000` (เปิดด้วยไฟล์ `start.bat`)

---

## สารบัญ
1. [แผนผังหน้าจอเว็บ (Visual Screen Wireframe)](#1-แผนผังหน้าจอเว็บ-visual-screen-wireframe)
2. [ชี้จุดอธิบายทีละส่วนบนหน้าเว็บ (Part-by-Part UI Guide)](#2-ชี้จุดอธิบายทีละส่วนบนหน้าเว็บ-part-by-part-ui-guide)
3. [ตารางเทียบ 19 ข้อคำถาม: ชี้จุดบนเว็บ & ตำแหน่งโค้ด (Exam Checklist)](#3-ตารางเทียบ-19-ข้อคำถาม-ชี้จุดบนเว็บ--ตำแหน่งโค้ด)
4. [สคริปต์แนวทางตอบคำถามอาจารย์ (Oral Defense Cheat Sheet)](#4-สคริปต์แนวทางตอบคำถามอาจารย์-oral-defense-cheat-sheet)

---

## 1. แผนผังหน้าจอเว็บ (Visual Screen Wireframe)

```
+-----------------------------------------------------------------------------------+
| [1] NAVBAR: โลโก้ Smart Library | แท็บ [คลังหนังสือ] [ยืม-คืน] [สมาชิก] [ตรวจสอบโครงงาน] | [Badge: Prisma Active] |
+-----------------------------------------------------------------------------------+
| [2] STATS CARDS:                                                                  |
|  [ 📚 หนังสือทั้งหมด ]    [ 👥 สมาชิกห้องสมุด ]   [ 🔄 กำลังยืมอยู่ ]   [  ส่งคืนสำเร็จ ]  |
|  (ตาราง books)          (ตาราง members)        (ตาราง borrow)       (status=returned) |
+-----------------------------------------------------------------------------------+
| [3] CONTROLS: [ ช่องค้นหา Real-time ] [ เลือกหมวดหมู่ ] [ รีเฟรช ] [+ เพิ่มหนังสือใหม่]  |
+-----------------------------------------------------------------------------------+
| [4] ตารางข้อมูลหลัก (MAIN TABLE):                                                  |
|  - แท็บ 1: ตารางคลังหนังสือ (id, title, author, isbn, category, quantity)          |
|  - แท็บ 2: ตารางประวัติยืม-คืน (id, book_id, member_id, borrow/return_date, status) |
|  - แท็บ 3: ตารางสมาชิก (id, name, email, phone, created_at)                        |
|  - แท็บ 4: หน้าจอตรวจสอบสถาปัตยกรรม & Live JSON API                                 |
+-----------------------------------------------------------------------------------+
| [5] FOOTER: ระบุ Tech Stack: Next.js (SSR+CSR), Tailwind CSS, Prisma ORM, MySQL   |
+-----------------------------------------------------------------------------------+
```

---

## 2. ชี้จุดอธิบายทีละส่วนบนหน้าเว็บ (Part-by-Part UI Guide)

### ส่วนที่ 1: แถบเมนูด้านบน (Navbar & Status Badge)
* **สิ่งที่เห็นบนหน้าจอ**:
  * โลโก้และชื่อเว็บ `Smart Library (Step 1 Spec)`
  * ปุ่มสลับแท็บ: คลังหนังสือ, ยืม-คืนหนังสือ, สมาชิก, ตรวจสอบโครงงานขั้นที่ 1
  * ป้ายไฟเขียวกะพริบ `Prisma Middleware Active`
* **ตรงกับแผนโครงงานข้อไหน**:
  * **หมวด Framework ข้อ 1, 2**: ใช้ Next.js ควบคุม Navbar และ Router
  * **หมวดแหล่งข้อมูล ข้อ 4**: แสดงสถานะว่า Web เชื่อมต่อผ่านตัวกลาง **Prisma ORM** ไม่ได้ต่อดิบเข้า Database
* **ไฟล์โค้ด**: [`components/Navbar.jsx`](./components/Navbar.jsx)

---

### ส่วนที่ 2: การ์ดตัวเลขสรุป 4 กล่อง (Stats Cards)
* **สิ่งที่เห็นบนหน้าจอ**:
  * กล่องที่ 1: `หนังสือทั้งหมด` (แสดงจำนวนเรื่อง และผลรวมจำนวนเล่มคงเหลือ)
  * กล่องที่ 2: `สมาชิกห้องสมุด` (แสดงจำนวนคน)
  * กล่องที่ 3: `กำลังยืมอยู่` (จำนวนรายการที่ยังไม่คืน)
  * กล่องที่ 4: `ส่งคืนสำเร็จ` (จำนวนรายการที่คืนแล้ว)
* **ตรงกับแผนโครงงานข้อไหน**:
  * **หมวดแหล่งข้อมูล ข้อ 1**: ดึงข้อมูลสรุปตรงมาจากทั้ง 3 ตารางในฐานข้อมูล `library_db` ได้แก่ `books`, `members` และ `borrow_records`
  * **หมวดสถาปัตยกรรม ข้อ 5, 6**: Front-end นำข้อมูลตัวเลขที่คำนวณจาก Back-end มาแสดงผลผ่าน React Component
* **ไฟล์โค้ด**: [`components/StatsCards.jsx`](./components/StatsCards.jsx)

---

### ส่วนที่ 3: หน้าคลังหนังสือ & การจัดการ (Book Catalog Tab)
* **สิ่งที่เห็นบนหน้าจอ**:
  * แถบค้นหา: พิมพ์ชื่อหนังสือ, ชื่อคนแต่ง หรือเลข ISBN เพื่อกรองผลทันทีแบบไม่ต้องกด Enter
  * ตัวเลือกหมวดหมู่ (Category Filter): เลือกดูเฉพาะ Computer Science, Software Engineering, AI ฯลฯ
  * ปุ่มสีน้ำเงิน `+ เพิ่มหนังสือใหม่`: กดแล้วจะมีหน้าต่าง Modal โผล่ขึ้นมาให้กรอกข้อมูล
  * ตารางหนังสือ: มีคอลัมน์ `id`, `title`, `author`, `isbn`, `category`, `quantity` พร้อมปุ่มรูปดินสอ (แก้ไข) และถังขยะ (ลบ)
* **ตรงกับแผนโครงงานข้อไหน**:
  * **หมวดแหล่งข้อมูล ข้อ 1**: โครงสร้างตารางและชื่อคอลัมน์ตรงกับตาราง `books` ใน `library_db`
  * **หมวดสถาปัตยกรรม ข้อ 7 (Client-side)**: การค้นหาแบบเรียลไทม์ การเปิด/ปิด Modal และการกดเปลี่ยนหมวดหมู่ ทำงานบนเบราว์เซอร์ (CSR) หน้าจอไม่กระพริบรีโหลด
  * **หมวดสถาปัตยกรรม ข้อ 4 (Back-end)**: เมื่อกด "บันทึก" หรือ "ลบ" ระบบจะส่งคำขอ HTTP POST / PUT / DELETE ไปที่ Next.js API เพื่อให้บันทึกลง MySQL
* **ไฟล์โค้ด**:
  * หน้ารวมและ State: [`components/LibraryDashboard.jsx`](./components/LibraryDashboard.jsx)
  * หน้าต่างกรอกข้อมูล: [`components/BookModal.jsx`](./components/BookModal.jsx)
  * หลังบ้าน API: [`app/api/books/route.js`](./app/api/books/route.js) และ [`app/api/books/[id]/route.js`](./app/api/books/[id]/route.js)

---

### ส่วนที่ 4: หน้าระบบยืม-คืนหนังสือ (Borrow & Return Tab)
* **สิ่งที่เห็นบนหน้าจอ**:
  * ปุ่มสีส้ม `+ บันทึกการยืมหนังสือ`: ให้เลือกสมาชิกผู้ยืม และเลือกหนังสือที่มีสต็อกเหลืออยู่
  * ตารางประวัติ: แสดงชื่อหนังสือ, ชื่อสมาชิก, วันที่ยืม, วันที่คืน, ป้ายสถานะ (`กำลังยืม`, `เกินกำหนด`, `คืนเรียบร้อย`)
  * ปุ่มสีเขียว `คืนหนังสือ`: อยู่ท้ายแถวของรายการที่กำลังยืม กดคืนได้ในคลิกเดียว
* **ตรงกับแผนโครงงานข้อไหน**:
  * **หมวดแหล่งข้อมูล ข้อ 1**: ตาราง `borrow_records` มี Foreign Key เชื่อมไปยัง `book_id` และ `member_id`
  * **หมวดสถาปัตยกรรม ข้อ 4 (Business Logic ใน Back-end)**: 
    * เมื่อกดยืม: Back-end ตรวจสอบว่า `quantity > 0` แล้วลดจำนวนเล่มลง 1 เล่ม
    * เมื่อกดคืน: Back-end คืนสถานะเป็น `returned`, บันทึกวันที่คืน และเพิ่มจำนวนเล่มคงเหลือกลับเข้าคลังอัตโนมัติ
* **ไฟล์โค้ด**:
  * หน้าต่างยืม: [`components/BorrowModal.jsx`](./components/BorrowModal.jsx)
  * หลังบ้าน API ยืม-คืน: [`app/api/borrow/route.js`](./app/api/borrow/route.js)

---

### ส่วนที่ 5: หน้ารายชื่อสมาชิก (Members Tab)
* **สิ่งที่เห็นบนหน้าจอ**:
  * ตารางรายชื่อสมาชิก: รหัสสมาชิก (id), ชื่อ-สกุล (name), อีเมล (email), เบอร์โทร (phone), วันที่สมัคร
  * ปุ่มสีเขียว `+ เพิ่มสมาชิกใหม่`: หน้าต่าง Modal รับข้อมูลสมาชิกใหม่
* **ตรงกับแผนโครงงานข้อไหน**:
  * **หมวดแหล่งข้อมูล ข้อ 1**: ตาราง `members` ใน `library_db`
  * **หมวด Framework ข้อ 4**: ดึงและบันทึกข้อมูลสมาชิกผ่าน `prisma.member.findMany()` และ `prisma.member.create()`
* **ไฟล์โค้ด**:
  * หน้าต่างเพิ่มสมาชิก: [`components/MemberModal.jsx`](./components/MemberModal.jsx)
  * หลังบ้าน API สมาชิก: [`app/api/members/route.js`](./app/api/members/route.js)

---

### ส่วนที่ 6: แท็บพิเศษ "ตรวจสอบโครงงานขั้นที่ 1" (Plan Spec Inspector)
* **สิ่งที่เห็นบนหน้าจอ**:
  * แบนเนอร์สีน้ำเงินเข้มสรุปความสอดคล้อง 100%
  * ปุ่มเมนูย่อย 3 แท็บ:
    1. **ตารางเทียบข้อกำหนด 3 หมวด**: สรุปแหล่งข้อมูล, Framework และ สถาปัตยกรรม
    2. **โครงสร้างฐานข้อมูล & Prisma Schema**: กล่องสีดำแสดงโค้ดไฟล์ `prisma/schema.prisma` แบบสด
    3. **Live API & SSR/CSR Verification**: กล่อง Terminal สด กดปุ่ม "รีเฟรชข้อมูล" เพื่อดูชุดข้อมูล JSON จริงที่ส่งจาก Back-end มายัง Front-end
* **ตรงกับแผนโครงงานข้อไหน**:
  * ออกแบบมาเพื่อเป็นหลักฐานแสดงแก่อาจารย์ผู้ตรวจว่า ทุกข้อในเอกสาร `D:\แผนโครงงาน_ขั้นที่1.md` มีโค้ดรันอยู่จริงในระบบ
* **ไฟล์โค้ด**: [`components/ArchitectureInspector.jsx`](./components/ArchitectureInspector.jsx) และ [`app/api/system-info/route.js`](./app/api/system-info/route.js)

---

## 3. ตารางเทียบ 19 ข้อคำถาม: ชี้จุดบนเว็บ & ตำแหน่งโค้ด

ตารางนี้เรียงตามลำดับคำถามทุกข้อในไฟล์ `D:\แผนโครงงาน_ขั้นที่1.md` เพื่อให้เปิดเทียบได้ทันที:

### หมวดที่ 1: หมวดแหล่งข้อมูล (Data Repository)

| ข้อที่ | คำถามตามเอกสาร | จุดที่ชี้บนหน้าเว็บ | ตำแหน่งไฟล์และโค้ด | คำตอบที่ใช้อธิบาย |
| :---: | :--- | :--- | :--- | :--- |
| **1** | **ฐานข้อมูลชื่ออะไร มีโครงสร้างอย่างไร** | • แท็บ "คลังหนังสือ" แสดงตาราง `books`<br>• แท็บ "สมาชิก" แสดงตาราง `members`<br>• แท็บ "ยืม-คืน" แสดงตาราง `borrow_records`<br>• แท็บ "ตรวจสอบโครงงาน" สรุป 3 ตาราง | [`prisma/schema.prisma`](./prisma/schema.prisma) บรรทัดที่ 14-49 | ฐานข้อมูลชื่อ **`library_db`** มี 3 ตารางหลัก คือ:<br>1. `books` (id, title, author, isbn, category, quantity)<br>2. `members` (id, name, email, phone)<br>3. `borrow_records` (id, book_id, member_id, borrow_date, return_date, status) |
| **2** | **ระบบฐานข้อมูลที่ใช้คือ** | แท็บ "ตรวจสอบโครงงาน" เมนูสรุปหมวดแหล่งข้อมูล ระบุ "MySQL (เชิงสัมพันธ์ / RDBMS)" | [`prisma/schema.prisma`](./prisma/schema.prisma#L8) | **MySQL (เชิงสัมพันธ์ / RDBMS)** มีความสัมพันธ์ระหว่างตารางผ่าน Foreign Key |
| **3** | **ซอฟต์แวร์ ระบบฐานข้อมูล คือ** | ข้อความกำกับในแท็บ "ตรวจสอบโครงงาน" | [`.env`](./.env) | **XAMPP / MySQL Server** (ทำงานที่พอร์ตมาตรฐาน 3306) |
| **4** | **MiddleWare(ตัวกลาง) ที่เป็น interface ระหว่าง Web Application กับ ฐานข้อมูล คือ** | ป้าย Badge สีเขียวมุมขวาบนของ Navbar: `Prisma Middleware Active` | [`lib/prisma.js`](./lib/prisma.js)<br>[`prisma/schema.prisma`](./prisma/schema.prisma) | **Prisma ORM** ทำหน้าที่แปลงคำสั่ง JavaScript/TS เป็น SQL Query และจัดการ Connection Pool |
| **5** | **Connection String ที่ MiddleWare แจ้งให้แก่ Web Application คือ** | แท็บ "ตรวจสอบโครงงาน" เมนู "โครงสร้างฐานข้อมูล" หรือดูไฟล์ `.env` | [`.env`](./.env) บรรทัดที่ 3 | `mysql://root:@localhost:3306/library_db` (เก็บไว้ในตัวแปร `DATABASE_URL` ในไฟล์ `.env`) |

---

### หมวดที่ 2: หมวด Framework การพัฒนา Web Application

| ข้อที่ | คำถามตามเอกสาร | จุดที่ชี้บนหน้าเว็บ | ตำแหน่งไฟล์และโค้ด | คำตอบที่ใช้อธิบาย |
| :---: | :--- | :--- | :--- | :--- |
| **6** | **Framework ที่ใช้ ชื่อ** | ข้อความใต้โลโก้บน Navbar และใน Footer | [`package.json`](./package.json#L12)<br>[`next.config.js`](./next.config.js) | **Next.js** (เวอร์ชัน 14 ใช้ App Router สถาปัตยกรรมล่าสุด) |
| **7** | **Framework ที่ใช้ มีความสามารถเป็น Back-end หรือ Front-end หรืออะไร** | บนหน้าเว็บมีทั้งส่วน UI (React) และมีการกดบันทึกแล้วยิง API (Route Handler) ภายในระบบเดียวกัน | • Front-end: [`components/`](./components)<br>• Back-end: [`app/api/`](./app/api) | **เป็นทั้งสองอย่าง (Full-stack Framework)** รองรับทั้งส่วนติดต่อผู้ใช้ (React) และ API Service หลังบ้าน |
| **8** | **Framework ที่ใช้ เกี่ยวข้องอะไรกับ Node.js** | แท็บ "ตรวจสอบโครงงาน" แสดง Runtime: Node.js | รันเซิร์ฟเวอร์ด้วยคำสั่ง `npm run dev` / Node.js | Next.js **ทำงานอยู่บน Node.js runtime** โดย Node.js ใช้รันโค้ดฝั่ง Server, API routes และกระบวนการ Build ทั้งหมด |
| **9** | **Framework ที่ใช้ เชื่อมต่อกับฐานข้อมูลได้อย่างไร** | ปุ่มบันทึกยืม/คืน หรือเพิ่มหนังสือ เมื่อกดแล้วข้อมูลเปลี่ยนทันที | บรรทัด `import { prisma } from '@/lib/prisma'` ใน [`app/api/books/route.js`](./app/api/books/route.js#L2) | เชื่อมต่อผ่าน **Prisma Client** โดยเรียกใช้ใน API Route หรือ Server Component แล้ว Prisma จะไปคุยกับ MySQL ให้ |
| **10** | **หลักฐานที่ Web App นี้ เชื่อมต่อตรงไปที่ฐานข้อมูล หรือ MiddleWare คือ** | ป้าย Badge สีเขียวบน Navbar และโค้ดในโฟลเดอร์ `app/api/` | ดูในไฟล์ [`lib/prisma.js`](./lib/prisma.js) | **เชื่อมผ่าน MiddleWare (Prisma)** ไม่ได้ใช้ raw MySQL socket เพื่อความปลอดภัย ป้องกัน SQL Injection และจัดการ Query ได้ง่าย |
| **11** | **Framework ที่ใช้ เลือกใช้ CSS framework ใด** | ดีไซน์ของหน้าเว็บ: ปุ่มมน (`rounded-2xl`), เงาการ์ด (`shadow-sm`), สีปุ่ม (`bg-blue-600`) | [`tailwind.config.js`](./tailwind.config.js)<br>[`app/globals.css`](./app/globals.css) | **Tailwind CSS** (จัดรูปแบบด้วย Utility Classes เช่น `flex`, `p-6`, `text-white`) |

---

### หมวดที่ 3: หมวดสถาปัตยกรรมของ Web Application

| ข้อที่ | คำถามตามเอกสาร | จุดที่ชี้บนหน้าเว็บ | ตำแหน่งไฟล์และโค้ด | คำตอบที่ใช้อธิบาย |
| :---: | :--- | :--- | :--- | :--- |
| **12** | **Framework ที่ใช้ สนับสนุนให้ web app ใช้สถาปัตยกรรมใดบ้าง (2 อย่าง)** | หน้าเว็บเปิดติดทันทีพร้อมข้อมูลตั้งต้น (SSR) และสามารถพิมพ์ค้นหาโต้ตอบได้ทันที (CSR) | • SSR: [`app/page.jsx`](./app/page.jsx)<br>• CSR: [`components/LibraryDashboard.jsx`](./components/LibraryDashboard.jsx) | สนับสนุน **Server-Side Rendering (SSR)** และ **Client-Side Rendering (CSR)** |
| **13** | **Back-end ที่สร้างด้วย Next.js ใช้ภาษาใด** | ดูในแท็บ "Live API & SSR/CSR Verification" | ไฟล์ `.js` ใน [`app/api/`](./app/api) | **JavaScript / TypeScript (รันบน Node.js)** |
| **14** | **Front-end ที่สร้างด้วย Next.js ใช้ภาษาใด** | หน้าตาและโครงสร้าง HTML ของเว็บทั้งหมด | ไฟล์ `.jsx` ใน [`components/`](./components) | **JavaScript / TypeScript ร่วมกับ JSX (React)** |
| **15** | **Back-end ทำหน้าที่อะไร** | เวลาเรากดปุ่ม "ยืมหนังสือ" สต็อกลดลงอัตโนมัติ | Logic ตรวจสอบสต็อกใน [`app/api/borrow/route.js`](./app/api/borrow/route.js#L54-L67) | **รับ Request, ประมวลผล Business Logic, ดึง/บันทึกข้อมูลผ่าน Prisma ไปยัง MySQL แล้วส่งข้อมูลกลับเป็น JSON** |
| **16** | **Front-end ทำหน้าที่อะไร** | ส่วนที่มองเห็นทั้งหมด เช่น ปุ่มกด, ช่องกรอก, ตาราง, หน้าต่าง Modal | [`components/LibraryDashboard.jsx`](./components/LibraryDashboard.jsx) | **แสดงผล UI, รับ Input จากผู้ใช้ และเรียก API (fetch) เพื่อขอหรือส่งข้อมูล** |
| **17** | **ข้อมูลจาก back-end นำไปใช้ใน front-end ได้อย่างไร** | แท็บ "ตรวจสอบโครงงาน" -> หน้าจอ Terminal แสดงกล่อง JSON สด | ฟังก์ชัน `refreshAllData()` ใน [`components/LibraryDashboard.jsx`](./components/LibraryDashboard.jsx#L41-L58) | **Back-end ส่งข้อมูล JSON ผ่าน API → Front-end ใช้คำสั่ง `fetch()` ดึงมาเก็บลงใน React State (`useState`) แล้วนำไปแสดงผลในคอมโพเนนต์** |
| **18** | **Client-side เป็นอย่างไร** | การพิมพ์ค้นหาหนังสือแล้วตารางกรองทันที, การสลับแท็บโดยไม่ต้องรีเฟรชหน้าเบราว์เซอร์ | คอมโพเนนต์ที่มีคำสั่ง `'use client'` ด้านบนสุด เช่น [`components/Navbar.jsx`](./components/Navbar.jsx#L1) | **เป็นโค้ดที่รันใน Web Browser ของผู้ใช้** ทำหน้าที่เกี่ยวกับ Interactivity, State, กรองข้อมูล และการอัปเดตหน้าจอโดยไม่ต้องโหลดหน้าเว็บใหม่ |
| **19** | **Server-side เป็นอย่างไร** | การเชื่อมต่อฐานข้อมูล MySQL, การรัน API Route Handlers | [`app/page.jsx`](./app/page.jsx) และโฟลเดอร์ [`app/api/`](./app/api) | **เป็นโค้ดที่รันอย่างปลอดภัยบน Server** ทำหน้าที่เชื่อมต่อฐานข้อมูล, จัดการความลับของระบบ และประมวลผล Logic ก่อนส่งผลลัพธ์ไปที่ Client |

---

## 4. สคริปต์แนวทางตอบคำถามอาจารย์ (Oral Defense Cheat Sheet)

หากอาจารย์ให้ชี้หน้าจอและอธิบาย สามารถพูดตามลำดับนี้ได้เลยครับ:

> **1. เปิดหน้าเว็บที่แท็บ "คลังหนังสือ":**  
> *"สวัสดีครับอาจารย์ หน้าเว็บนี้คือ **ระบบจัดการห้องสมุด** พัฒนาด้วย **Next.js** และจัดสไตล์ด้วย **Tailwind CSS** ครับ โดยหน้าจอนี้แสดงข้อมูลจากตาราง `books` ซึ่งในฐานข้อมูล `library_db` ของเรามีทั้งหมด 3 ตารางหลัก ได้แก่ `books`, `members` และ `borrow_records` ตามแผนโครงงานขั้นที่ 1 ครับ"*

> **2. สาธิตการเชื่อมต่อและ Middleware:**  
> *"สังเกตที่มุมขวาบนจะมี Badge แสดงสถานะ **Prisma Middleware Active** แปลว่าเว็บของเราไม่ได้เชื่อมตรงไปยัง MySQL แต่เชื่อมผ่านตัวกลางคือ **Prisma ORM** โดยกำหนดค่า Connection String ไว้ในไฟล์ `.env` เพื่อความปลอดภัยและง่ายต่อการคิวรีข้อมูลครับ"*

> **3. สาธิตการทำงาน Front-end & Client-Side (CSR):**  
> *"เมื่อผมลองพิมพ์ค้นหาชื่อหนังสือในช่องค้นหา หรือกดเปลี่ยนหมวดหมู่ จะเห็นว่าหน้าจออัปเดตทันทีโดยไม่ต้องรีโหลดหน้าเว็บ นี่คือการทำงานของฝั่ง **Front-end** ที่เขียนด้วย **React JSX** และเป็นรูปแบบ **Client-Side Rendering (CSR)** ครับ"*

> **4. สาธิตการทำงาน Back-end & Logic ยืม-คืน:**  
> *"เมื่อผมกดสลับไปที่แท็บ 'ยืม-คืนหนังสือ' แล้วกดยืมหนังสือ ระบบจะส่งคำขอไปยัง **Back-end API Route** ซึ่งเขียนด้วย **JavaScript บน Node.js runtime** ฝั่ง Back-end จะตรวจสอบว่ามีหนังสือเหลือหรือไม่ แล้วสั่งให้ Prisma บันทึกการยืมและตัดสต็อกหนังสือลง 1 เล่ม แล้วตอบกลับมาเป็น **JSON** เพื่อให้หน้าเว็บนำไปอัปเดตตัวเลขในการ์ดสรุปสถิติด้านบนครับ"*

> **5. เปิดแท็บ "ตรวจสอบโครงงานขั้นที่ 1":**  
> *"และในแท็บสุดท้ายนี้ ผมได้ทำหน้าจอสรุปการตรวจสอบสถาปัตยกรรมไว้ โดยแสดงทั้งตารางเทียบข้อกำหนด 3 หมวดหมู่, โค้ด `schema.prisma` ที่ใช้กำหนดโครงสร้างตารางจริง และมี Live JSON Terminal แสดงข้อมูลที่ Back-end ส่งให้ Front-end แบบเรียลไทม์ ครบถ้วนตรงตามข้อกำหนดของขั้นที่ 1 ทุกประการครับ"*
